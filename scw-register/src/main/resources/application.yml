server:
  port: 8761

spring:
  application:
    name: SCW-REGISTER

eureka:
  client:
    service-url:
      defaultZone: http://localhost:${server.port}/eureka
    fetch-registry: false #不从其他注册中心获取信息
    register-with-eureka: false #不在其他注册中心进行注册
